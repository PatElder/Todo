{{#each this}}
<div class="task{{#if isComplete}} complete{{/if}}" id="task-{{id}}">
	<div class="taskName">{{name}}</div>
	<div class="taskCompleted">
		<button class="complete-task" data-id="{{id}}">Complete</button>
	</div>
	<div class="taskDelete">
		<button class="delete-task" data-id="{{id}}">Delete</button>
	</div>
		<div class="taskEdit">
		<button class="edit-task" data-id="{{id}}">Edit</button>
		<textarea class="edit-form" rows="4" cols="50"  data-id="{{id}}"> {{description}}</textarea>
		<button class="updateDescriptionButton"  data-id="{{id}}">Update</button>
	</div>
	{{#if description}}
		<div class="taskDescription">{{description}}</div>
	{{/if}}
</div>
{{/each}}